<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Teach.me</title>
</head>
<body>
<div class="container">
<sidebar class="sidebar">
 <details open class="suggestion"> 
    <summary>Tópicos Sugeridos</summary>
    <button>Html</button>
    <button>CSS</button>
    <button>JavaScript</button>
    <button>TypeScript</button>

 </details>

 <details> 
    <summary>Históricos</summary>
    <button>Java</button>
    <button>PHP</button>
 

 </details>
</sidebar>

<div class="content">
    <div class="box-chat">
        <h1>Voce esta estudando <span>HTML</span></h1>
            <div class="question">
                <h2>O atributo id é usado para atribuir um identificador único a um elemento HTML. Cada id deve ser único dentro de uma página HTML, o que significa que um id só pode ser aplicado a um único elemento.</h2>
                <p></p>O atributo id é usado para atribuir um identificador único a um elemento HTML. Cada id deve ser único dentro de uma página HTML, o que significa que um id só pode ser aplicado a um único elemento.
            </div>

            <div class="feedback">
                <h2>feedback teach<span>.me</span></h2>
                <p>O atributo id é usado para atribuir um identificador único a um elemento HTML. Cada id deve ser único dentro de uma página HTML, o que significa que um id só pode ser aplicado a um único elemento.</p>
            </div>
            <div class="action"><button>Estudar novo Topico</button></div>
        
    <footer class="box-footer">
            <p>teach <span>.me</span></p>
        </footer>
    </div>
    </div> 
</div>    
</body>
</html>


style.css


.box-chat{
    display: flex;
    flex-direction: column;
    gap: 15px;
    height: 180%;
    padding-top: 30px;
}

.box-chat > h1 {
    font-size: 2.5rem;
    font-weight: 400;
}

.box-chat > span{
    color: var(--primary);
}

.question{
    width: 100%;
    background-color: var(--foreground-primary);
    padding: 20px 15px;
    border: 1px solid var(--border-primary);
    border-radius: 4px;
}

.question p{
    font-weight: 300px;
}
.question h2{
    font-size: 1.5rem;
    font-size: 500;
    margin-bottom: 15px;
    color: var(--text-color-question);
}

.answer{
    width: 100%;
    background-color: var(--foreground-primary);
    border: 1px solid var(--border-primary);
    border-radius: 4px;
}

.answer h2{
    padding: 10px 15px;
    background-color: var(--secondary);
    border: 1px solid var(--border-primary);
    font-weight: 400;

}

.answer p{
    padding: 20px 15px;
    font-weight: 300px;
}

.feedback{
    width: 100%;
    padding: 20px 15px;
    background-color: var(--foreground-secondary);
    border: 1px solid var(--border-secondary);
    border-radius: 4px;
}

.feedback h2{
    font-size: 1.8rem;
    text-align: center;
    margin-bottom: 20px;
}

.feedback p{
    margin-bottom: 20px;
    font-weight: 300px;
}

.feedback .action{
    display: flex;
    justify-content: center;
    gap: 20px;
    padding-top: 20px;
    border-top: 1px solid var(--border-secondary);
}

.feedback .action button{
    width: 170px;
    padding: 0px;
    border-radius: 4px;
    background-color: var(--primary);
    color: var(--text-color);
}
